spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
     url: jdbc:h2:mem:testdb
     username: sa
     password:
  sql:
    init:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

